questions:
- variable: subdomain
  type: string
  required: true
  default: "changeme"
  label: "Subdomain for icedc.se"
  description: "The subdomain where the Jupyter instance will be located"
  group: "Jupyter settings"
- variable: token
  type: string
  required: true
  default: "changeme"
  label: "Jupyter token"
  description: "A unique token used to protect the Jupyter instance"
  group: "Jupyter settings"
